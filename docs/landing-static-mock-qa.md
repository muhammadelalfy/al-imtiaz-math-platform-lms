# Static Mock Landing Verification

The landing was reviewed in the managed preview at desktop (`1280×1100`) and mobile (`390×844`) viewports after removal of mock-targeted GSAP motion. The dashboard illustration, floating mock status cards, full-platform artwork, and demonstration counter values remain static once rendered.

The non-mock value strip and package section retain the restrained one-time ScrollTrigger entrance behavior. The static Arabic demo values remain visibly labelled as illustrative and do not represent actual clients, learners, attendance, payments, reviews, or performance data.
