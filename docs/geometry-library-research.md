## Fresh browser PDF artifact

The fresh browser export `exam-1 (1).pdf` was generated by jsPDF as a two-page A4 image PDF. Visual inspection confirms the Arabic title/headings remain readable, option checkboxes remain aligned, and question watermarks are light, contained, and no longer bold or intersecting the question text. Page 2 contains question 4 with the rectangle SVG, dimension arrows, labels `٦ سم` and `٤ سم`, and the caption `مستطيل بأبعاد معلومة`. The image-PDF has no selectable text by design because it captures the exact rendered browser paper.
